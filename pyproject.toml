[project]
name = "keychecker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Dennis Burrows", email = "dkburrows72@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "requests>=2.32.5",
]

[project.scripts]
keychecker = "keychecker:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
